async function cargarPropiedades(){let propiedades=[];try{const rawData=await api.fetchProperties();propiedades=rawData.map(p=>{const tipo=(p.tipo_activo||'').toLowerCase();const esVivienda=(tipo==='casa'||tipo==='departamento');return{id:p.id,categoria:esVivienda?'vivienda':'terreno',estado:p.estado||'Disponible',titulo:p.titulo,precio:typeof p.precio==='number'?'$'+parseFloat(p.precio).toLocaleString():(p.precio||'$0'),imagen:p.imagen_url||p.imagen||'https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=800&q=80',area:typeof p.area_m2==='number'?p.area_m2+' m²':(p.area_m2||p.area||'0 m²'),habitaciones:p.habitaciones||'0',banos:p.banos||'0',zonificacion:p.zonificacion||'Residencial',parametros:p.parametros||'Estándar',descripcion:p.descripcion||'Propiedad disponible. Contáctenos para más información.'};});return propiedades.filter(p=>p.estado!=='Vendido');}catch(err){console.error("Error al cargar propiedades de la nube:",err);return[];}}
let coleccion=[];const gridContenedor=document.getElementById('grid-propiedades');const modal=document.getElementById('property-modal');const body=document.body;function showToast(message){const container=document.getElementById('toast-container');if(!container)return;const toast=document.createElement('div');toast.className='toast';toast.textContent=message;container.appendChild(toast);setTimeout(()=>toast.remove(),4000);}
function actualizarContadores(){const total=coleccion.length;const viviendas=coleccion.filter(p=>p.categoria==='vivienda').length;const terrenos=coleccion.filter(p=>p.categoria==='terreno').length;if(document.getElementById('count-todos'))document.getElementById('count-todos').textContent=total;if(document.getElementById('count-vivienda'))document.getElementById('count-vivienda').textContent=viviendas;if(document.getElementById('count-terreno'))document.getElementById('count-terreno').textContent=terrenos;}
function renderizarColeccion(datos){if(!gridContenedor)return;gridContenedor.innerHTML='';if(!datos||datos.length===0){gridContenedor.innerHTML=`<div class="empty-state"style="text-align: center; padding: 60px 20px; width: 100%; grid-column: 1 / -1;"><div class="empty-icon"style="font-size: 40px; margin-bottom: 15px;">🏠</div><h3 style="color: #fff; margin-bottom: 8px; font-size: 20px;">No hay inmuebles publicados actualmente</h3><p style="font-size: 14px; color: var(--text-muted);">Las propiedades disponibles aparecerán automáticamente aquí.</p></div>`;return;}
datos.forEach(prop=>{let specsHTML=prop.categoria==='vivienda'?`<span>📐 ${prop.area}</span><span>🛏️ ${prop.habitaciones}Hab</span><span>🛁 ${prop.banos}Baños</span>`:`<span>📐 ${prop.area}</span><span>📍 ${prop.zonificacion}</span><span>🏗️ ${prop.parametros}</span>`;gridContenedor.innerHTML+=`<article class="property-card"onclick="abrirModal(${prop.id})"><div class="property-img"><div class="tag-status">${prop.estado}</div><img src="${prop.imagen}"alt="${prop.titulo}"loading="lazy"></div><div class="property-info"><div class="property-price">${prop.precio}</div><h3 class="property-title">${prop.titulo}</h3><div class="property-specs">${specsHTML}</div></div></article>`;});}
function filtrarPorCategoria(categoria){document.querySelectorAll('.filter-controls .btn-outline').forEach(btn=>btn.classList.remove('btn-active'));const btnActivo=document.getElementById('btn-'+categoria);if(btnActivo)btnActivo.classList.add('btn-active');renderizarColeccion(categoria==='todos'?coleccion:coleccion.filter(prop=>prop.categoria===categoria));const portafolioSection=document.getElementById('portafolio');if(portafolioSection){portafolioSection.scrollIntoView({behavior:'smooth'});}}
function abrirModal(id){const prop=coleccion.find(p=>p.id==id);if(!prop||!modal)return;document.getElementById('modal-img').style.backgroundImage=`url('${prop.imagen}')`;document.getElementById('modal-title').innerText=prop.titulo;document.getElementById('modal-price').innerText=prop.precio;document.getElementById('modal-desc').innerText=prop.descripcion;let specsHTML=`<div><strong>Área Total</strong><br>${prop.area}</div>`;if(prop.categoria==='vivienda'){specsHTML+=`<div><strong>Habitaciones</strong><br>${prop.habitaciones}</div><div><strong>Baños</strong><br>${prop.banos}</div>`;}else{specsHTML+=`<div><strong>Zonificación</strong><br>${prop.zonificacion}</div><div><strong>Parámetros</strong><br>${prop.parametros}</div>`;}
document.getElementById('modal-specs').innerHTML=specsHTML;modal.classList.add('active');body.style.overflow='hidden';}
function cerrarModal(){if(modal){modal.classList.remove('active');body.style.overflow='';}}
if(modal){modal.addEventListener('click',(e)=>{if(e.target===modal)cerrarModal();});}
async function enviarCRM(origenMarca){const btnSubmit=document.querySelector('#contacto form button');const nombre=document.getElementById('crm-nombre')?.value;const email=document.getElementById('crm-email')?.value;const prefijo=document.getElementById('crm-prefijo')?.value;const telefono=document.getElementById('crm-telefono')?.value;const interes=document.getElementById('crm-interes')?.value;const emailRegex=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;if(!nombre){showToast('⚠️ Ingrese su nombre completo.');return;}
if(!email||!emailRegex.test(email)){showToast('⚠️ Ingrese un correo electrónico válido.');return;}
if(!telefono||telefono.length!==9){showToast('⚠️ Ingrese un celular válido de 9 dígitos.');return;}
if(!interes){showToast('⚠️ Seleccione una opción de interés.');return;}
if(btnSubmit){btnSubmit.disabled=true;btnSubmit.innerHTML='Enviando...';}
if(typeof fbq==='function'){fbq('track','Lead');}
const payload={nombre_completo:nombre,telefono:prefijo+' '+telefono,email:email,estado_lead:'Nuevo',origen:origenMarca,tipo_interes:interes,fecha_registro:new Date().toISOString().split('T')[0]};const success=await api.saveClient(payload);if(btnSubmit){btnSubmit.disabled=false;btnSubmit.innerHTML='Solicitar Asesoría Personalizada';}
if(success){showToast('✅ ¡Datos recibidos! Un asesor se comunicará pronto.');const wsMsg=`Hola,%20soy%20${encodeURIComponent(nombre)}.%20Me%20interesa%20información%20sobre%20${encodeURIComponent(interes)}`;setTimeout(()=>{window.open(`https:},1000);document.getElementById('crm-nombre').value='';document.getElementById('crm-email').value='';document.getElementById('crm-telefono').value='';document.getElementById('crm-interes').value='';}else{showToast('❌ Ocurrió un error al enviar tu solicitud. Intenta nuevamente.');}}
async function initApp(){coleccion=await cargarPropiedades();renderizarColeccion(coleccion);actualizarContadores();if(typeof supabaseClient!=='undefined'&&supabaseClient){try{supabaseClient.channel('public:propiedades').on('postgres_changes',{event:'*',schema:'public',table:'propiedades'},async()=>{console.log("⚡ Actualizando catálogo en tiempo real desde Supabase Cloud...");coleccion=await cargarPropiedades();renderizarColeccion(coleccion);actualizarContadores();}).subscribe();}catch(e){console.error("Error al suscribirse al canal de Supabase:",e);}}}
document.addEventListener('DOMContentLoaded',initApp);const moduleObserver=new IntersectionObserver((entries,observer)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target);}});},{root:null,rootMargin:'0px',threshold:0.15});document.querySelectorAll('.fade-module').forEach(modulo=>moduleObserver.observe(modulo));let currentSlide=0;const slides=document.querySelectorAll('.carousel-img');const track=document.getElementById('carouselTrack');const dotsContainer=document.getElementById('carouselDots');if(slides.length>0&&track&&dotsContainer){slides.forEach((_,index)=>{const dot=document.createElement('button');dot.classList.add('carousel-dot');if(index===0)dot.classList.add('active');dot.addEventListener('click',()=>goToSlide(index));dotsContainer.appendChild(dot);});const dots=document.querySelectorAll('.carousel-dot');function updateDots(){dots.forEach((dot,index)=>{dot.classList.toggle('active',index===currentSlide);});}
function goToSlide(index){currentSlide=index;if(currentSlide>=slides.length)currentSlide=0;if(currentSlide<0)currentSlide=slides.length-1;track.style.transform=`translateX(-${currentSlide*100}%)`;updateDots();}
function moveCarousel(direction){goToSlide(currentSlide+direction);}
let carouselInterval=setInterval(()=>moveCarousel(1),4000);const carouselContainer=document.querySelector('.hero-video');if(carouselContainer){carouselContainer.addEventListener('mouseenter',()=>clearInterval(carouselInterval));carouselContainer.addEventListener('mouseleave',()=>{clearInterval(carouselInterval);carouselInterval=setInterval(()=>moveCarousel(1),4000);});}}